{
  "name": "events-benchmarks",
  "version": "1.0.0",
  "description": "Performance benchmarks for @logistically/events library",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "benchmark": "node dist/index.js",
    "benchmark:throughput": "node dist/throughput-benchmark.js",
    "benchmark:memory": "node dist/memory-benchmark.js",
    "benchmark:latency": "node dist/latency-benchmark.js",
    "benchmark:concurrent": "node dist/concurrent-benchmark.js",
    "benchmark:e2e": "node dist/e2e-benchmark.js",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@logistically/events": "file:../",
    "@nestjs/microservices": "^10.0.0",
    "ioredis": "^5.6.1",
    "uuid": "^11.1.0",
    "zod": "^4.0.5",
    "performance-now": "^2.1.0",
    "cli-progress": "^3.12.0",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "@types/cli-progress": "^3.11.5",
    "typescript": "^5.8.3",
    "jest": "^30.0.4",
    "ts-jest": "^29.4.0"
  }
}

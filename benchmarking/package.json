{
  "name": "@logistically/events-benchmarking",
  "version": "1.0.0",
  "description": "Benchmarking framework for @logistically/events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "benchmark": "ts-node src/cli.ts",
    "test:simple": "ts-node src/simple-consumer-test.ts",
    "test:minimal": "ts-node src/minimal-latency-test.ts",
    "benchmark:redis": "ts-node src/cli.ts --transport redis",
    "benchmark:memory": "ts-node src/cli.ts --transport memory",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@logistically/events": "file:../"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "commander": "^11.0.0",
    "chalk": "^5.0.0",
    "ora": "^7.0.0",
    "cli-table3": "^0.6.0"
  },
  "peerDependencies": {
    "@logistically/events": "*"
  }
}
